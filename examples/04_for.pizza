sauce print(x);

for i = 0, i < 5 in
  print(i); # prints 0 to 4

sauce printchar(x);

base printstar(n)
  for i = 1, i < n, 1.0 in
    printchar(42);  # ascii 42 = '*'

printstar(100); # print 100 '*' characters
